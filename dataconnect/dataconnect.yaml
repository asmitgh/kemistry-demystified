specVersion: "v1beta"
serviceId: "kemistry (1)"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "kemistry (1)-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
